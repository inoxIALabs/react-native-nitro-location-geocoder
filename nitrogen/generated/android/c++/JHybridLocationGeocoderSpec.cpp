///
/// JHybridLocationGeocoderSpec.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridLocationGeocoderSpec.hpp"

// Forward declaration of `LocationGeocoderResult` to properly resolve imports.
namespace margelo::nitro::locationgeocoder { struct LocationGeocoderResult; }

#include "LocationGeocoderResult.hpp"
#include <NitroModules/Promise.hpp>
#include <NitroModules/JPromise.hpp>
#include "JLocationGeocoderResult.hpp"
#include <string>

namespace margelo::nitro::locationgeocoder {

  std::shared_ptr<JHybridLocationGeocoderSpec> JHybridLocationGeocoderSpec::JavaPart::getJHybridLocationGeocoderSpec() {
    auto hybridObject = JHybridObject::JavaPart::getJHybridObject();
    auto castHybridObject = std::dynamic_pointer_cast<JHybridLocationGeocoderSpec>(hybridObject);
    if (castHybridObject == nullptr) [[unlikely]] {
      throw std::runtime_error("Failed to downcast JHybridObject to JHybridLocationGeocoderSpec!");
    }
    return castHybridObject;
  }

  jni::local_ref<JHybridLocationGeocoderSpec::CxxPart::jhybriddata> JHybridLocationGeocoderSpec::CxxPart::initHybrid(jni::alias_ref<jhybridobject> jThis) {
    return makeCxxInstance(jThis);
  }

  std::shared_ptr<JHybridObject> JHybridLocationGeocoderSpec::CxxPart::createHybridObject(const jni::local_ref<JHybridObject::JavaPart>& javaPart) {
    auto castJavaPart = jni::dynamic_ref_cast<JHybridLocationGeocoderSpec::JavaPart>(javaPart);
    if (castJavaPart == nullptr) [[unlikely]] {
      throw std::runtime_error("Failed to cast JHybridObject::JavaPart to JHybridLocationGeocoderSpec::JavaPart!");
    }
    return std::make_shared<JHybridLocationGeocoderSpec>(castJavaPart);
  }

  void JHybridLocationGeocoderSpec::CxxPart::registerNatives() {
    registerHybrid({
      makeNativeMethod("initHybrid", JHybridLocationGeocoderSpec::CxxPart::initHybrid),
    });
  }

  // Properties
  

  // Methods
  std::shared_ptr<Promise<LocationGeocoderResult>> JHybridLocationGeocoderSpec::reverseGeocode(double latitude, double longitude, const std::string& locale) {
    static const auto method = _javaPart->javaClassStatic()->getMethod<jni::local_ref<JPromise::javaobject>(double /* latitude */, double /* longitude */, jni::alias_ref<jni::JString> /* locale */)>("reverseGeocode");
    auto __result = method(_javaPart, latitude, longitude, jni::make_jstring(locale));
    return [&]() {
      auto __promise = Promise<LocationGeocoderResult>::create();
      __result->cthis()->addOnResolvedListener([=](const jni::alias_ref<jni::JObject>& __boxedResult) {
        auto __result = jni::static_ref_cast<JLocationGeocoderResult>(__boxedResult);
        __promise->resolve(__result->toCpp());
      });
      __result->cthis()->addOnRejectedListener([=](const jni::alias_ref<jni::JThrowable>& __throwable) {
        jni::JniException __jniError(__throwable);
        __promise->reject(std::make_exception_ptr(__jniError));
      });
      return __promise;
    }();
  }

} // namespace margelo::nitro::locationgeocoder
